The workspace is a mounted volume used to store project data between the graphcap server and the host machine.

The workspace is mounted in the ``docker-compose.yml`` file and is defined as follows:

.. code-block:: yaml

    
